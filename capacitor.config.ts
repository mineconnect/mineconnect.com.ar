import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tuempresa.mineconnectsat',
  appName: 'MineConnectSAT',
  webDir: 'dist'
};

export default config;
